import React from 'react'

const layout = () => {
  return (
    <div>Navigate to top</div>
  )
}

export default layout

// This layout component in the page file will be 
// present on every page with /post in the URL.
// it can be /post, /post/newpost, /post/:postId. 